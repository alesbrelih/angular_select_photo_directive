# angular SELECT custom directive with bootstrap

Selects photo id from a list of photos and its titles. Acts like a Select component. 
But in select component you cant have a photo inside select option body.
